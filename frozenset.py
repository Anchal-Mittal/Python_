"""
Frozen Sets Frozen sets are immutable objects that only support methods and 
operators that produce a result without a?ecting the frozen set or sets to which
they are applied.
"""

def fun():
    frozen_set=frozenset(["aeiou","g","j"])
    print(frozen_set)
fun();
